# ===========================================
# CONFIGURAÇÕES DA API
# ===========================================
# URL base da API backend (sem o prefixo /api, pois os endpoints já incluem /api/v1)
# Dica: para acesso via outras máquinas na rede, evite "localhost" (pois vira o localhost DO CLIENTE).
# Você pode:
# - Remover/omitir VITE_API_URL para o app derivar automaticamente do host atual (IP/hostname) + porta, ou
# - Definir explicitamente para o IP/hostname do servidor.
#VITE_API_URL=http://localhost:3001

# Porta padrão do backend quando VITE_API_URL estiver ausente (ou estiver em localhost e o cliente acessar via IP)
VITE_API_PORT=3001

# ===========================================
# CONFIGURAÇÕES DA APLICAÇÃO
# ===========================================
# Nome da aplicação
VITE_APP_NAME=Tutoriais Lukos
VITE_APP_VERSION=1.0.0
VITE_APP_DESCRIPTION=Plataforma de tutoriais do sistema Lukos

# ===========================================
# CONFIGURAÇÕES DE DESENVOLVIMENTO
# ===========================================
# Modo de desenvolvimento (true/false)
VITE_DEV_MODE=true

# Usar dados mockados em desenvolvimento
VITE_USE_MOCK_DATA=false

# ===========================================
# CONFIGURAÇÕES DE AUTENTICAÇÃO
# ===========================================
# Usuários da equipe (formato: user:hash:name:role,user2:hash2:name2:role2)
# Para gerar hash: node -e "const bcrypt=require('bcryptjs');bcrypt.hash('senha',10).then(h=>console.log(h))"
# Exemplo: VITE_AUTH_USERS=admin:$2a$10$hash...:Administrador:admin,editor:$2a$10$hash...:Editor:editor
VITE_AUTH_USERS=

# Tempo de expiração do token (em horas)
VITE_TOKEN_EXPIRATION=24

# ===========================================
# CONFIGURAÇÕES DE UPLOAD
# ===========================================
# Tamanho máximo de arquivo (em MB)
VITE_MAX_FILE_SIZE=10

# Tipos de arquivo permitidos
VITE_ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,video/mp4,video/webm

# ===========================================
# CONFIGURAÇÕES DE NOTIFICAÇÕES
# ===========================================
# Habilitar notificações push
VITE_ENABLE_PUSH_NOTIFICATIONS=false

# ===========================================
# CONFIGURAÇÕES DE ANALYTICS
# ===========================================
# Google Analytics ID (opcional)
# VITE_GA_TRACKING_ID=GA-XXXXXXXXX

# ===========================================
# CONFIGURAÇÕES DE REDE
# ===========================================
# Timeout para requisições (em ms)
VITE_REQUEST_TIMEOUT=10000

# Número máximo de tentativas de reconexão
VITE_MAX_RETRY_ATTEMPTS=3