# Servidor
PORT=3001

# Banco de Dados SQL Server - Prisma
# Recomendado: host + porta (evita problemas com instância nomeada + escapes do dotenv em Windows)
DATABASE_URL="sqlserver://localhost:1443;database=tutoriaislukos;user=sa;password=sua_senha;trustServerCertificate=true"
#
# ⚠️ Se você tentar usar instância nomeada em Windows, cuidado com `\t` dentro de aspas duplas:
# - dotenv pode interpretar `\t` como TAB e corromper o host (ex: `localhost\tutoriaislukos`).
# - O `backend/src/config/database.js` também espera `:porta` no formato atual.
# Se ainda precisar de instância nomeada, prefira host:porta ou use aspas simples e/ou escape extra:
# DATABASE_URL='sqlserver://localhost\\tutoriaislukos:1443;database=tutoriaislukos;user=sa;password=sua_senha;trustServerCertificate=true'
# DATABASE_URL="sqlserver://localhost\\\\tutoriaislukos:1443;database=tutoriaislukos;user=sa;password=sua_senha;trustServerCertificate=true"

# Dev helper: cria o banco automaticamente se não existir (padrão: habilitado fora de production).
# DB_AUTO_CREATE=false

# Variáveis antigas (mantidas para compatibilidade, mas não usadas pelo Prisma)
DB_SERVER=localhost\tutoriaislukos
DB_PORT=1443
DB_NAME=tutoriaislukos
DB_USER=sa
DB_PASSWORD=sua_senha
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# CORS
CORS_ORIGIN=http://localhost:5173

# JWT
JWT_SECRET=seu_secret_jwt_aqui
JWT_EXPIRES_IN=24h
REFRESH_TOKEN_SECRET=seu_refresh_secret_aqui
REFRESH_TOKEN_EXPIRES_IN=7d